# NyraHost Phase 1 runtime deps (D-15). Bundled as wheels under
# Binaries/Win64/NyraHost/wheels/ for offline install (D-14).
#
# Generated from pyproject.toml [project.dependencies] via pip-compile
# --generate-hashes on a Windows dev machine; hashes omitted in this
# committed copy because the offline install path uses --find-links
# (local wheel cache) + --no-index, not PyPI index lookups.
#
# Any bump here must be mirrored in pyproject.toml [project.dependencies]
# in the same commit; version drift between the two is a Rule-1 bug.
mcp==1.2.0
websockets==12.0
httpx==0.27.0
pydantic==2.7.4
pydantic-core==2.18.4
anyio==4.3.0
sniffio==1.3.1
httpcore==1.0.5
h11==0.14.0
idna==3.7
certifi==2024.7.4
structlog==24.1.0
annotated-types==0.7.0
typing-extensions==4.12.2
pywin32==306 ; sys_platform == 'win32'

# Phase 8 PARITY-01: document attachment parsers (LOCKED-06 amended 2026-05-10).
# pure-Python core libraries; lxml + Pillow are transitive C-extensions but
# ship pre-built Windows-x64 wheels (zero source-build on the offline cache).
# Estimated wheel-cache delta: ~9.1 MB (well under T-08-02's 75 MB ceiling).
pypdf==6.11.0
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
markdown==3.10.2
beautifulsoup4==4.14.3
# Transitive — pinned to current Windows x64 wheel versions verified 2026-05-10.
lxml==6.1.0          ; sys_platform == 'win32'   # python-docx + python-pptx
Pillow==12.2.0       ; sys_platform == 'win32'   # python-pptx (image extraction)
soupsieve==2.8.3                                 # beautifulsoup4
XlsxWriter==3.2.9                                # python-pptx (xlsx output paths)
et-xmlfile==2.0.0                                # openpyxl
